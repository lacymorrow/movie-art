name: CI

on:
  push:
    branches: [master, main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  smoke:
    name: Smoke (Node ${{ matrix.node }} on ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        node: ["18", "20", "22"]
        include:
          - os: macos-latest
            node: "20"

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node }}
          cache: npm

      - run: npm install

      # The full ava suite hits TMDB and is occasionally flaky on rate limits.
      # CI verifies the module loads and the CLI bin is present.
      - name: Module loads
        run: node -e "const a = require('./'); if (typeof a !== 'function') process.exit(1)"

      - name: CLI entry is executable
        run: node -e "require('fs').accessSync('./cli.js')"
